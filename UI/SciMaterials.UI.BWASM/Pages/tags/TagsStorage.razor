@page "/tags_storage"

@if (_Tags is null)
{
    <MudChip Color="Color.Warning">loading...</MudChip>
}
else
{
    @foreach (var tag in _Tags)
    {
        <MudChip Color="Color.Secondary" OnClick="() => OnTagClick(tag)">@tag.Name</MudChip>
    }
}
