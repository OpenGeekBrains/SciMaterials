@page "/tags_storage"

@if (_Tags is null)
{
    <MudSkeleton SkeletonType="SkeletonType.Rectangle" />
}
else
{
    @foreach (var tag in _Tags)
    {
        <MudChip Color="Color.Secondary" OnClick="() => OnTagClick(tag.Id)">#@tag.Name</MudChip>
    }
}
